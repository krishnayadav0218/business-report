pandas>=2.0
matplotlib>=3.7
python-pptx>=0.6.23
python-dotenv>=1.0
openpyxl>=3.1
Flask>=3.0
gunicorn>=21.2
APScheduler>=3.10
requests>=2.31
pdfplumber>=0.11
python-docx>=1.1

# Used only if DATABASE_URL is set (optional persistent settings storage -- see README)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# Used only if SENTRY_DSN is set (optional error monitoring -- see README)
sentry-sdk[flask]>=2.0

# Uncomment based on your scheduled-pipeline data source (src/main.py):
# gspread>=6.0             # Google Sheets
# oauth2client>=4.1        # Google Sheets auth
# pymysql>=1.1             # MySQL (in addition to SQLAlchemy above)
